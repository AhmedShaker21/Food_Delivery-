import 'package:flutter/material.dart';

class ViewMorePopular extends StatelessWidget {
  const ViewMorePopular({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
